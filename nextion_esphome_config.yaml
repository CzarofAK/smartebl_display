# =============================================================================
# Smart EBL - ESPHome Configuration
# =============================================================================
# Central electrical control unit for motorhomes/campervans
# Hardware: ESP32-WROVER-IE-N16R8 with MCP23017 IO expander
# Author: Ingenieurbüro Jan Morawek, Munich
# =============================================================================

substitutions:
  name: smart-ebl
  friendly_name: Smart EBL

esphome:
  name: ${name}
  friendly_name: ${friendly_name}
  platformio_options:
    board_build.flash_mode: dio
    board_build.f_flash: 80000000L
  on_boot:
    priority: -100  # Run early, before other components initialize
    then:
      - output.turn_off: lin_ms # OFF=Slave, ON=Master
      - output.turn_off: lin_wake # OFF=Normal, ON=Wakeup
      - output.turn_off: lin_slp # OFF=Normal, ON=Sleep
      - output.turn_on: can_term # Terminating resistor enabled after boot if ON
      - output.turn_off: can_stb # Transceiver active after boot if OFF

esp32:
  board: esp-wrover-kit
  flash_size: 16MB
  framework:
    type: esp-idf

# Include external basics (wifi, ota, api, etc.)
<<: !include .basics.yaml

# =============================================================================
# External Components
# =============================================================================

external_components:
  # Custom ADS7830 8-bit ADC component
  - source:
      type: local
      path: components
    components: [ads7830]

  # Truma iNetbox LIN integration
#  - source:
#      type: git
#      url: https://github.com/skrebber/esphome-truma_inetbox
#      ref: main
#    components: [truma_inetbox]

# =============================================================================
# I2C Bus Configuration
# =============================================================================
# All ADCs (4x ADS7830) and MCP23017 IO expander share this bus

i2c:
  sda: GPIO21
  scl: GPIO22
  scan: true
  frequency: 100kHz

# =============================================================================
# MCP23017 IO Expander (U17) - Relay Control
# =============================================================================
# I2C Address: 0x20 (A0=A1=A2=GND)
# GPIO33 = IO_RESET (reset pin), GPIO36 = INT_A, GPIO39 = INT_B
# INT_A and INT_B are not used in this configuration as there are no inputs
# PORT B (GPB0-GPB7) - Relay coil control via ULN2803A:
#   GPB0=P_off, GPB1=P_on, GPB2=L_off, GPB3=L_on,
#   GPB4=A_off, GPB5=A_on, GPB6=12_off, GPB7=12_on
#
# PORT A (GPA0-GPA5) - Additional outputs:
#   GPA0=EisX, GPA1=D+2, GPA2=F-CTRL, GPA3=T1_DC, GPA4=T2_DC, GPA5=T3_DC

mcp23017:
  - id: io_expander
    address: 0x20

output:

  - platform: gpio
    id: lin_ms
    pin: GPIO25
    # OFF=Slave, ON=Master
  - platform: gpio
    id: lin_wake
    pin: GPIO26
    # OFF=Normal, ON=Wakeup
  - platform: gpio
    id: lin_slp
    pin: GPIO27
    # OFF=Normal, ON=Sleep

    # CAN Bus Control Pins (via SN65HVD234 U3)
  - platform: gpio
    id: can_term
    pin: GPIO23
    # Terminating resistor enabled after boot if ON
  - platform: gpio
    id: can_stb
    pin: GPIO32
    # Transceiver active after boot if OFF

# =============================================================================
# ADS7830 8-bit ADC Hubs
# =============================================================================

ads7830:
  - id: adc_fuse_group1
    address: 0x48   # U4 - Fuses F1-F8
  - id: adc_fuse_group2
    address: 0x49   # U5 - Fuses F9-F16
  - id: adc_inputs
    address: 0x4A   # U6 - Tank sensors, inputs
  - id: adc_outputs
    address: 0x4B   # U7 - Output monitoring, battery

# =============================================================================
# CAN Bus (RV-C Protocol)
# =============================================================================
# GPIO14 = RXD_CAN1, GPIO15 = TXD_CAN1, GPIO23 = CAN_TERM, GPIO32 = CAN_1_STB

canbus:
  - platform: esp32_can
    id: can_bus
    tx_pin: GPIO15
    rx_pin: GPIO14
    can_id: 0
    bit_rate: 250kbps

# =============================================================================
# UART Configuration
# =============================================================================

uart:
  # LIN Bus for Truma CP Plus (via TJA1021T U14)
  # GPIO12 = TXD1, GPIO13 = RXD1
  - id: lin_uart
    tx_pin: GPIO12
    rx_pin: GPIO13
    baud_rate: 9600
    data_bits: 8
    parity: NONE
    stop_bits: 2

  # Nextion Display (via TRS3221 RS-232 transceiver U18)
  # GPIO18 = TXD2, GPIO19 = RXD2
  - id: display_uart
    tx_pin: GPIO18
    rx_pin: GPIO19
    baud_rate: 115200
    data_bits: 8
    parity: NONE
    stop_bits: 1

logger:
  level: WARN
  baud_rate: 0 #switch off USB-Logger
  logs:
    api.service: WARN
    i2c.idf: WARN
    gpio.output: WARN
    uptime.sensor: WARN
    sensor: WARN
    sensor.filter: WARN
    ads7830: WARN
    nextion: ERROR          # Nextion-Kommunikation
    homeassistant: WARN
    wifi: WARN
    component: ERROR
    text_sensor: NONE
    canbus: NONE

# =============================================================================
# NEXTION Display Configuration
# =============================================================================

display:
  - platform: nextion
    id: nextion_display
    uart_id: display_uart
    update_interval: 500ms
    on_page:
      then:
        - globals.set:
            id: current_page
            value: !lambda 'return (int)x;'

    # Lambda for regular updates (runs every update_interval)
    lambda: |-
      // Status bar updates (time, date, temperatures) - always updating
      it.set_component_text("txt_status_bar", id(status_text).state.c_str());

      // === LEVELS PAGE UPDATES ===
      // Freshwater tank (LOCAL SmartEBL sensor: tank1_level) - only on current page = 3 (Levels_1)
      if (id(current_page) == 3) {
        it.set_component_value("bar_fresh", id(tank1_level).state);
        it.set_component_text_printf("val_fresh", "%.0f%%", id(tank1_level).state);
        // Global Store
        it.send_command_printf("g_bar_fresh.val=%d", (int)id(tank1_level).state);
        it.send_command_printf("g_val_fresh.txt=\"%.0f%%\"", id(tank1_level).state);

        // Greywater tank (LOCAL SmartEBL sensor: tank2_level)
        it.set_component_value("bar_grey", id(tank2_level).state);
        it.set_component_text_printf("val_grey", "%.0f%%", id(tank2_level).state);
        // Global Store
        it.send_command_printf("g_bar_grey.val=%d", (int)id(tank2_level).state);
        it.send_command_printf("g_val_grey.txt=\"%.0f%%\"", id(tank2_level).state);

        // Diesel tank (EXTERNAL from Home Assistant)
        it.set_component_value("bar_diesel", id(diesel_level).state);
        it.set_component_text_printf("val_diesel", "%.0f%%", id(diesel_level).state);
        // Global Store
        it.send_command_printf("g_bar_diesel.val=%d", (int)id(diesel_level).state);
        it.send_command_printf("g_val_diesel.txt=\"%.0f%%\"", id(diesel_level).state);

        // Gas 1 tank (EXTERNAL from Home Assistant)
        it.set_component_value("bar_gas1", id(gas1_level).state);
        it.set_component_text_printf("val_gas1", "%.0f%%", id(gas1_level).state);
        // Global Store
        it.send_command_printf("g_bar_gas1.val=%d", (int)id(gas1_level).state);
        it.send_command_printf("g_val_gas1.txt=\"%.0f%%\"", id(gas1_level).state);

        // Gas 2 tank (EXTERNAL from Home Assistant)
        it.set_component_value("bar_gas2", id(gas2_level).state);
        it.set_component_text_printf("val_gas2", "%.0f%%", id(gas2_level).state);
        // Global Store
        it.send_command_printf("g_bar_gas2.val=%d", (int)id(gas2_level).state);
        it.send_command_printf("g_val_gas2.txt=\"%.0f%%\"", id(gas2_level).state);
      }

      // === LEVELS 2 PAGE UPDATES ===
      // Freshwater tank (LOCAL SmartEBL sensor: tank1_level) - only on current page = 4 (Levels_2)
      if (id(current_page) == 4) {
        it.set_component_value("bar_fresh_big", id(tank1_level).state);
        it.set_component_text_printf("val_fresh_big", "%.0f%%", id(tank1_level).state);
        // Global Store
        it.send_command_printf("g_bar_fresh.val=%d", (int)id(tank1_level).state);
        it.send_command_printf("g_val_fresh.txt=\"%.0f%%\"", id(tank1_level).state);

        // Greywater tank (LOCAL SmartEBL sensor: tank2_level)
        it.set_component_value("bar_grey_big", id(tank2_level).state);
        it.set_component_text_printf("val_grey_big", "%.0f%%", id(tank2_level).state);
        // Global Store
        it.send_command_printf("g_bar_grey.val=%d", (int)id(tank2_level).state);
        it.send_command_printf("g_val_grey.txt=\"%.0f%%\"", id(tank2_level).state);

        // Update pump button state to match LOCAL SmartEBL pump switch
        it.set_component_value("btn_pump", id(switch_pump).state);
        // Global Store
        it.send_command_printf("g_pump_state.val=%d", (int)id(switch_pump).state);
      }

# =============================================================================
# Truma iNetbox Integration
# =============================================================================

#truma_inetbox:
#  id: truma
#  uart_id: lin_uart

# =============================================================================
# Sensors - Fuse Voltage Monitoring
# =============================================================================

sensor:

  # ---------------------------------------------------------------------------
  # Display Sensors
  # ---------------------------------------------------------------------------

  # Diesel tank - TODO: Add to SmartEBL or specify external source
  - platform: homeassistant
    id: diesel_level
    entity_id: input_number.diesel_tank_level  # TODO: Verify entity_id
    internal: true

  # Gas 1 tank - TODO: Add to SmartEBL or specify external source
  - platform: homeassistant
    id: gas1_level
    entity_id: input_number.gas1_tank_level  # TODO: Verify entity_id
    internal: true

  # Gas 2 tank - TODO: Add to SmartEBL or specify external source
  - platform: homeassistant
    id: gas2_level
    entity_id: input_number.gas2_tank_level  # TODO: Verify entity_id
    internal: true

  # Outside temperature - TODO: Specify source
  - platform: homeassistant
    id: outside_temperature
    entity_id: input_number.outside_temperatur  # TODO: Verify entity_id
    internal: true

  # Inside temperature - TODO: Specify source
  - platform: homeassistant
    id: inside_temperature
    entity_id: input_number.inside_temperatur  # TODO: Verify entity_id
    internal: true

  # ---------------------------------------------------------------------------
  # Fuse Group 1 (U4 - 0x48) - Fuses F1-F8
  # ---------------------------------------------------------------------------

  - platform: ads7830
    ads7830_id: adc_fuse_group1
    channel: 7
    name: "Fuse F1 Voltage"
    id: fuse_f1_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62  # Voltage divider calibration

  - platform: ads7830
    ads7830_id: adc_fuse_group1
    channel: 6
    name: "Fuse F2 Voltage"
    id: fuse_f2_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_fuse_group1
    channel: 5
    name: "Fuse F3 Voltage"
    id: fuse_f3_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_fuse_group1
    channel: 4
    name: "Fuse F4 Voltage"
    id: fuse_f4_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_fuse_group1
    channel: 3
    name: "Fuse F5 Voltage"
    id: fuse_f5_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_fuse_group1
    channel: 2
    name: "Fuse F6 Voltage"
    id: fuse_f6_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_fuse_group1
    channel: 1
    name: "Fuse F7 Voltage"
    id: fuse_f7_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_fuse_group1
    channel: 0
    name: "Fuse F8 Voltage"
    id: fuse_f8_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  # ---------------------------------------------------------------------------
  # Fuse Group 2 (U5 - 0x49) - Fuses F9-F16
  # ---------------------------------------------------------------------------

  - platform: ads7830
    ads7830_id: adc_fuse_group2
    channel: 0
    name: "Fuse F9 Voltage"
    id: fuse_f9_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_fuse_group2
    channel: 1
    name: "Fuse F10 Voltage"
    id: fuse_f10_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_fuse_group2
    channel: 2
    name: "Fuse F11 Voltage"
    id: fuse_f11_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_fuse_group2
    channel: 3
    name: "Fuse F12 Voltage"
    id: fuse_f12_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_fuse_group2
    channel: 4
    name: "Fuse F13 Voltage"
    id: fuse_f13_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_fuse_group2
    channel: 5
    name: "Fuse F14 Voltage"
    id: fuse_f14_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_fuse_group2
    channel: 6
    name: "Fuse F15 Voltage"
    id: fuse_f15_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_fuse_group2
    channel: 7
    name: "Fuse F16 Voltage"
    id: fuse_f16_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  # ---------------------------------------------------------------------------
  # Input Monitoring (U6 - 0x4A)
  # ---------------------------------------------------------------------------

  - platform: ads7830
    ads7830_id: adc_inputs
    channel: 0
    name: "Tank 1 Level"
    id: tank1_level
    unit_of_measurement: "%"
    accuracy_decimals: 0
    update_interval: 10s
    filters:
      - multiply: 0.392  # Scale 0-255 to 0-100%

  - platform: ads7830
    ads7830_id: adc_inputs
    channel: 1
    name: "Tank 2 Level"
    id: tank2_level
    unit_of_measurement: "%"
    accuracy_decimals: 0
    update_interval: 10s
    filters:
      - multiply: 0.392

  - platform: ads7830
    ads7830_id: adc_inputs
    channel: 2
    name: "Tank 3 Level"
    id: tank3_level
    unit_of_measurement: "%"
    accuracy_decimals: 0
    update_interval: 10s
    filters:
      - multiply: 0.392

  - platform: ads7830
    ads7830_id: adc_inputs
    channel: 3
    name: "Voltage 3v3 Bus"
    id: voltage_3v3_bus
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 1.60

  - platform: ads7830
    ads7830_id: adc_inputs
    channel: 4
    name: "AC Charger Detection"
    id: ac_charger_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_inputs
    channel: 5
    name: "DuoControl Status"
    id: duocontrol_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_inputs
    channel: 6
    name: "D+ Signal Status"
    id: dplus_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_inputs
    channel: 7
    name: "F10 ABS Status"
    id: f10_abs_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  # ---------------------------------------------------------------------------
  # Output Monitoring (U7 - 0x4B)
  # ---------------------------------------------------------------------------

  - platform: ads7830
    ads7830_id: adc_outputs
    channel: 0
    name: "Leisure Battery Voltage"
    id: leisure_battery_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_outputs
    channel: 1
    name: "Starter Battery Voltage"
    id: starter_battery_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_outputs
    channel: 2
    name: "Light Output Voltage"
    id: light_output_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_outputs
    channel: 3
    name: "12V User Output Voltage"
    id: user_12v_output_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_outputs
    channel: 4
    name: "12V AUX Output Voltage"
    id: aux_12v_output_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_outputs
    channel: 5
    name: "Pump Output Voltage"
    id: pump_output_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_outputs
    channel: 6
    name: "Light L Output Voltage"
    id: light_l_output_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  - platform: ads7830
    ads7830_id: adc_outputs
    channel: 7
    name: "Contour Light Output Voltage"
    id: contour_light_output_voltage
    unit_of_measurement: "V"
    accuracy_decimals: 2
    update_interval: 5s
    filters:
      - multiply: 6.62

  # ---------------------------------------------------------------------------
  # Truma Sensors
  # ---------------------------------------------------------------------------

#  - platform: truma_inetbox
#    truma_inetbox_id: truma
#    type: CURRENT_ROOM_TEMPERATURE
#    name: "Truma Room Temperature"
#    id: truma_room_temp

#  - platform: truma_inetbox
#    truma_inetbox_id: truma
#    type: CURRENT_WATER_TEMPERATURE
#    name: "Truma Water Temperature"
#    id: truma_water_temp

#  - platform: truma_inetbox
#    truma_inetbox_id: truma
#    type: HEATER_ERROR_CODE
#    name: "Truma Error Code"
#    id: truma_error_code

  # ---------------------------------------------------------------------------
  # System Diagnostics
  # ---------------------------------------------------------------------------

  - platform: internal_temperature
    name: "ESP32 Temperature"
    update_interval: 60s

  - platform: uptime
    name: "Uptime"
    update_interval: 60s

  - platform: wifi_signal
    name: "WiFi Signal"
    update_interval: 60s

# =============================================================================
# Binary Sensors
# =============================================================================

binary_sensor:

  # ---------------------------------------------------------------------------
  # Display Binary Sensors
  # ---------------------------------------------------------------------------

  - platform: nextion
    page_id: 4
    component_id: 27
    name: "Pump Button"
    internal: true
    on_press:
      - switch.toggle: switch_pump

  # ---------------------------------------------------------------------------
  # Fuse Status (OK when voltage > 1V)
  # ---------------------------------------------------------------------------

  - platform: template
    name: "Fuse F1 OK"
    id: fuse_f1_ok
    lambda: return id(fuse_f1_voltage).state > 1.0;
    device_class: power

  - platform: template
    name: "Fuse F2 OK"
    id: fuse_f2_ok
    lambda: return id(fuse_f2_voltage).state > 1.0;
    device_class: power

  - platform: template
    name: "Fuse F3 OK"
    id: fuse_f3_ok
    lambda: return id(fuse_f3_voltage).state > 1.0;
    device_class: power

  - platform: template
    name: "Fuse F4 OK"
    id: fuse_f4_ok
    lambda: return id(fuse_f4_voltage).state > 1.0;
    device_class: power

  - platform: template
    name: "Fuse F5 OK"
    id: fuse_f5_ok
    lambda: return id(fuse_f5_voltage).state > 1.0;
    device_class: power

  - platform: template
    name: "Fuse F6 OK"
    id: fuse_f6_ok
    lambda: return id(fuse_f6_voltage).state > 1.0;
    device_class: power

  - platform: template
    name: "Fuse F7 OK"
    id: fuse_f7_ok
    lambda: return id(fuse_f7_voltage).state > 1.0;
    device_class: power

  - platform: template
    name: "Fuse F8 OK"
    id: fuse_f8_ok
    lambda: return id(fuse_f8_voltage).state > 1.0;
    device_class: power

  - platform: template
    name: "Fuse F9 OK"
    id: fuse_f9_ok
    lambda: return id(fuse_f9_voltage).state > 1.0;
    device_class: power

  - platform: template
    name: "Fuse F10 OK"
    id: fuse_f10_ok
    lambda: return id(fuse_f10_voltage).state > 1.0;
    device_class: power

  - platform: template
    name: "Fuse F11 OK"
    id: fuse_f11_ok
    lambda: return id(fuse_f11_voltage).state > 1.0;
    device_class: power

  - platform: template
    name: "Fuse F12 OK"
    id: fuse_f12_ok
    lambda: return id(fuse_f12_voltage).state > 1.0;
    device_class: power

  - platform: template
    name: "Fuse F13 OK"
    id: fuse_f13_ok
    lambda: return id(fuse_f13_voltage).state > 1.0;
    device_class: power

  - platform: template
    name: "Fuse F14 OK"
    id: fuse_f14_ok
    lambda: return id(fuse_f14_voltage).state > 1.0;
    device_class: power

  - platform: template
    name: "Fuse F15 OK"
    id: fuse_f15_ok
    lambda: return id(fuse_f15_voltage).state > 1.0;
    device_class: power

  - platform: template
    name: "Fuse F16 OK"
    id: fuse_f16_ok
    lambda: return id(fuse_f16_voltage).state > 1.0;
    device_class: power

  # ---------------------------------------------------------------------------
  # Input Detection
  # ---------------------------------------------------------------------------

  - platform: template
    name: "D+ Active"
    id: dplus_active
    lambda: return id(dplus_voltage).state > 6.0;
    device_class: power

  - platform: template
    name: "Shore Power Connected"
    id: shore_power_connected
    lambda: return id(ac_charger_voltage).state > 6.0;
    device_class: plug

  # ---------------------------------------------------------------------------
  # Truma Binary Sensors
  # ---------------------------------------------------------------------------

#  - platform: truma_inetbox
#    truma_inetbox_id: truma
#    type: CP_PLUS_CONNECTED
#    name: "Truma CP Plus Connected"
#    id: truma_connected

#  - platform: truma_inetbox
#    truma_inetbox_id: truma
#    type: HEATER_HAS_ERROR
#    name: "Truma Has Error"
#    id: truma_has_error

# =============================================================================
# Text Sensors
# =============================================================================

  # ---------------------------------------------------------------------------
  # Display Text Sensors
  # ---------------------------------------------------------------------------

text_sensor:
  # Status bar text (time, date, temperatures)
  - platform: template
    id: status_text
    update_interval: 1s
    lambda: |-
      char buffer[100];
      auto time = id(homeassistant_time).now();
      auto inside_temp = id(inside_temperature).state;
      auto outside_temp = id(outside_temperature).state;

      sprintf(buffer, "%02d:%02d:%02d  %02d.%s.%02d     In: %.1f" "\xb0" "C  Out: %.1f" "\xb0" "C",
        time.hour, time.minute, time.second,
        time.day_of_month, time.strftime("%b").c_str(), time.year % 100,
        inside_temp, outside_temp);

      return {buffer};

# =============================================================================
# Switches - MCP23017 Controlled Relays
# =============================================================================

switch:
  # ---------------------------------------------------------------------------
  # Internal MCP23017 Pin Outputs (coil drivers via ULN2803A)
  # Bistable relays need a pulse to change state
  # PIN MAPPING FROM information/I2C overview:
  #   PORT B (pins 8-15): Relay coils
  #     GPB0(8)=P_off, GPB1(9)=P_on, GPB2(10)=L_off, GPB3(11)=L_on
  #     GPB4(12)=A_off, GPB5(13)=A_on, GPB6(14)=12_off, GPB7(15)=12_on
  #   PORT A (pins 0-7): Additional outputs
  #     GPA0(0)=EisX, GPA1(1)=D+2, GPA2(2)=F-CTRL
  #     GPA3(3)=T1_DC, GPA4(4)=T2_DC, GPA5(5)=T3_DC
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # Relay Coils MCP23017 Outputs (GPB0-GPB5)
  # ---------------------------------------------------------------------------

  # Pump relay coils (GPB0=OFF, GPB1=ON)
  - platform: gpio
    id: relay_pump_off_coil
    pin:
      mcp23xxx: io_expander
      number: 8  # GPB0 = P_off
      mode: OUTPUT
    internal: true
    on_turn_on:
      - delay: 100ms
      - switch.turn_off: relay_pump_off_coil

  - platform: gpio
    id: relay_pump_on_coil
    pin:
      mcp23xxx: io_expander
      number: 9  # GPB1 = P_on
      mode: OUTPUT
    internal: true
    on_turn_on:
      - delay: 100ms
      - switch.turn_off: relay_pump_on_coil

  # Light Group relay coils (GPB2=OFF, GPB3=ON)
  - platform: gpio
    id: relay_light_off_coil
    pin:
      mcp23xxx: io_expander
      number: 10  # GPB2 = L_off
      mode: OUTPUT
    internal: true
    on_turn_on:
      - delay: 100ms
      - switch.turn_off: relay_light_off_coil

  - platform: gpio
    id: relay_light_on_coil
    pin:
      mcp23xxx: io_expander
      number: 11  # GPB3 = L_on
      mode: OUTPUT
    internal: true
    on_turn_on:
      - delay: 100ms
      - switch.turn_off: relay_light_on_coil

  # AUX Group relay coils (GPB4=OFF, GPB5=ON)
  - platform: gpio
    id: relay_aux_off_coil
    pin:
      mcp23xxx: io_expander
      number: 12  # GPB4 = A_off
      mode: OUTPUT
    internal: true
    on_turn_on:
      - delay: 100ms
      - switch.turn_off: relay_aux_off_coil

  - platform: gpio
    id: relay_aux_on_coil
    pin:
      mcp23xxx: io_expander
      number: 13  # GPB5 = A_on
      mode: OUTPUT
    internal: true
    on_turn_on:
      - delay: 100ms
      - switch.turn_off: relay_aux_on_coil

  # 12V Group relay coils (GPB6=OFF, GPB7=ON)
  - platform: gpio
    id: relay_12v_off_coil
    pin:
      mcp23xxx: io_expander
      number: 14  # GPB6 = 12_off
      mode: OUTPUT
    internal: true
    on_turn_on:
      - delay: 100ms
      - switch.turn_off: relay_12v_off_coil

  - platform: gpio
    id: relay_12v_on_coil
    pin:
      mcp23xxx: io_expander
      number: 15  # GPB7 = 12_on
      mode: OUTPUT
    internal: true
    on_turn_on:
      - delay: 100ms
      - switch.turn_off: relay_12v_on_coil

  # ---------------------------------------------------------------------------
  # Additional MCP23017 Outputs (GPA0-GPA5)
  # ---------------------------------------------------------------------------

  # EisEx heater control (GPA0)
  - platform: gpio
    id: eisex_output
    name: "EisEx Heater"
    icon: mdi:heating-coil
    pin:
      mcp23xxx: io_expander
      number: 0  # GPA0 = EisX
      mode: OUTPUT

  # D+2 Relay control (GPA1)
  - platform: gpio
    id: dplus_relay
    name: "D+2 Relay"
    icon: mdi:car-battery
    pin:
      mcp23xxx: io_expander
      number: 1  # GPA1 = D+2
      mode: OUTPUT

  # Fridge control (GPA2)
  - platform: gpio
    id: fridge_control
    name: "Fridge Control"
    icon: mdi:fridge
    pin:
      mcp23xxx: io_expander
      number: 2  # GPA2 = F-CTRL
      mode: OUTPUT

  # Tank DC mode controls (GPA3-GPA5)
  # These inhibit tank readings while driving/moving
  - platform: gpio
    id: tank1_dc_control
    name: "Tank 1 Control"
    icon: mdi:water
    pin:
      mcp23xxx: io_expander
      number: 3  # GPA3 = T1_DC
      mode: OUTPUT

  - platform: gpio
    id: tank2_dc_control
    name: "Tank 2 Control"
    icon: mdi:water
    pin:
      mcp23xxx: io_expander
      number: 4  # GPA4 = T2_DC
      mode: OUTPUT

  - platform: gpio
    id: tank3_dc_control
    name: "Tank 3 Control"
    icon: mdi:water
    pin:
      mcp23xxx: io_expander
      number: 5  # GPA5 = T3_DC
      mode: OUTPUT

  # ---------------------------------------------------------------------------
  # User-Facing Template Switches (for Home Assistant)
  # ---------------------------------------------------------------------------

  - platform: template
    id: switch_pump
    name: "Water Pump"
    icon: mdi:water-pump
    optimistic: true
    turn_on_action:
      - switch.turn_on: relay_pump_on_coil
    turn_off_action:
      - switch.turn_on: relay_pump_off_coil

  # Light Group
  - platform: template
    name: "Light Group"
    id: switch_light
    icon: mdi:lightbulb-group
    optimistic: true
    turn_on_action:
      - switch.turn_on: relay_light_on_coil
    turn_off_action:
      - switch.turn_on: relay_light_off_coil

  # 12V Group
  - platform: template
    name: "12V Group"
    id: switch_12v
    icon: mdi:power-socket-eu
    optimistic: true
    turn_on_action:
      - switch.turn_on: relay_12v_on_coil
    turn_off_action:
      - switch.turn_on: relay_12v_off_coil

  # AUX Group
  - platform: template
    name: "AUX Group"
    id: switch_aux
    icon: mdi:power
    optimistic: true
    turn_on_action:
      - switch.turn_on: relay_aux_on_coil
    turn_off_action:
      - switch.turn_on: relay_aux_off_coil

# =============================================================================
# Climate - Truma Heater
# =============================================================================

#climate:
#  - platform: truma_inetbox
#    truma_inetbox_id: truma
#    type: ROOM
#    name: "Truma Heater"
#    id: truma_heater

# =============================================================================
# Number Controls - Truma Settings
# =============================================================================

#number:
#  - platform: truma_inetbox
#    truma_inetbox_id: truma
#    type: TARGET_ROOM_TEMPERATURE
#    name: "Truma Target Room Temp"
#    id: truma_target_room_temp

#  - platform: truma_inetbox
#    truma_inetbox_id: truma
#    type: TARGET_WATER_TEMPERATURE
#    name: "Truma Target Water Temp"
#    id: truma_target_water_temp

# =============================================================================
# Select Controls - Truma Modes
# =============================================================================

#select:
#  - platform: truma_inetbox
#    truma_inetbox_id: truma
#    type: HEATER_FAN_MODE_COMBI
#    name: "Truma Fan Mode"
#    id: truma_fan_mode

#  - platform: truma_inetbox
#    truma_inetbox_id: truma
#    type: HEATER_ENERGY_MIX_GAS
#    name: "Truma Energy Mix"
#    id: truma_energy_mix

globals:
  - id: current_page
    type: int
    initial_value: '0'
